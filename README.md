# Technical Writer Portfolio

This is a React + Vite portfolio site.

Quick start:

1. Install dependencies:

   npm install

2. Run the dev server:

   npm run dev

3. Build for production:

   npm run build

Repository setup and push instructions are in the project root docs or can be performed as follows (see CONTRIBUTING.md for longer guides).